# Project-specific R8 rules.
# Keep this file present because the release build references it from
# build.gradle.kts. Add narrowly scoped rules here only when R8 proves they are
# required; avoid broad keep rules that disable shrinking.
