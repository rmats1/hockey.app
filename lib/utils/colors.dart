import 'package:flutter/material.dart';

class AppColors {
  // Colores principales
  static const Color primary = Color(0xFF1B5E20);      // Verde hockey
  static const Color primaryLight = Color(0xFF4C8C4A); // Verde claro
  static const Color primaryDark = Color(0xFF003300);  // Verde oscuro
  
  // Colores secundarios
  static const Color secondary = Color(0xFFFFA000);    // Naranja/dorado
  static const Color secondaryLight = Color(0xFFFFD149);
  
  // Colores de fondo
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Colors.white;
  
  // Colores de texto
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textLight = Colors.white;
  
  // Colores de estado
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFF44336);
  static const Color warning = Color(0xFFFF9800);
  static const Color info = Color(0xFF2196F3);
}
